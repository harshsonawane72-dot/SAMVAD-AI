/**
 * SAMVAD AI — simulated AI analysis (rule / keyword based)
 *
 * IMPORTANT:
 * - This is NOT a real AI model or clinical tool.
 * - Output is for decision SUPPORT only.
 * - Must NOT diagnose mental health conditions.
 * - Must NOT make autonomous legal or emergency decisions.
 * - A human operator must review all results.
 */

const SimulateAI = {
  /**
   * Analyze statement text and return a demo result object:
   * { svi, riskLevel, indicators, recommendations }
   * Implementation will use simple heuristics + DEMO_DATA.
   */
  analyze(statementText) {
    // Placeholder — real simulation logic comes in a later step
    return {
      svi: null,
      riskLevel: null,
      indicators: [],
      recommendations: [],
      disclaimer:
        "Simulated advisory output only. Not a diagnosis. Human review required.",
    };
  },
};
